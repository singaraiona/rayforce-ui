;; Connect to server
(set h (hopen "localhost:5110"))

;; Create grid
(set g (widget {type: 'grid name: "Quotes"}))

;; Fetch and display data
(draw g (write h "quotes"))

(timer 1000 10000000 (fn [x] (draw g (write h "(take quotes -100)"))))
